# 生成物 / 本地状态
__pycache__/
*.pyc
.codebuddy/

# 持久化实例（可由 scripts/ingest_bend.py 重建；harness 用临时目录，不依赖此目录）
data/

# Python 虚拟环境
venv/
.venv/

# 编辑器 / OS
.vscode/
.idea/
.DS_Store

# 密钥 / 本地环境（MOD-deploy P0-1：凭证仅经 env 注入，禁止提交）
.env
.env.local
.env.*.local
*.local
secrets/

# Chroma 向量库运行时产物（测试/调试产生，不入仓）
*.chroma/
chroma.sqlite3
none/
