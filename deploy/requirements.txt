# 端侧实例依赖（Docker 构建用，= 完整依赖）
# Windows 直装请使用 requirements-light.txt 或 requirements-full.txt
# Python 3.11
pydantic>=2.0,<3.0
pyyaml>=6.0,<7.0
chromadb>=0.5.0,<0.6.0
httpx>=0.27.0,<1.0
# 真实中文语义嵌入（端侧 CPU 可跑；如仅用 mock 嵌入可省略）
# 同时其 CrossEncoder 直接用于加载开源重排器 BAAI/bge-reranker-v2-m3（独立重排阶段）
sentence-transformers>=3.0,<4.0
